---
- name: Installing apache2
  apt:
    name: "{{ item }}"
    state: latest
  loop:
    - apache2
    - php7.0
    - libapache2-mod-php7.0
    - php7.0-pgsql

- name: Setting up Drupal
  copy:
    src: var/www/drupal
    dest: /var/www/
    owner: www-data
    group: www-data
    mode: 0755  
